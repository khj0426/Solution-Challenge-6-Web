const msg = {
  sucessMain: 'Good job!',
  successBody: 'Misson Completed !',
  loginsuccess: 'Great!',
  loginsuccessBody: 'Login success!',
};

const user = {
  userimgURL: 'imgURL',
};

Object.freeze(msg);
Object.freeze(user);

export { user, msg };
